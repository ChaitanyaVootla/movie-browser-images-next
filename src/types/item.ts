export enum ItemType {
    Movie = "movie",
    SERIES = "series",
}

export enum ImageFormat {
    WEBP = "webp",
    AVIF = "avif",
}

export enum ImageType {
    POSTER = "poster",
    BACKDROP = "backdrop",
    WIDE_POSTER = "widePoster",
    LOGO = "logo",
}
